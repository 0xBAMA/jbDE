#version 430
layout( local_size_x = 16, local_size_y = 16, local_size_z = 1 ) in;

main() {
	// iterative process to get data at the highest possible resolution

	// postprocessing

	// write out to the texture, which will be sampled by the tonemapping

}